{% liquid
  assign product = product | default: closest.product
%}

<div>{{ product.selected_or_first_available_variant | money }}</div>

{% schema %}
{
  "name": "t:blocks.price",
  "settings": [
  ],
  "blocks": [],
  "presets": [
    {
      "name": "t:blocks.price",
      "category": "t:categories.product"
    }
  ]
}
{% endschema %}