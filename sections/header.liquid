{% style %}
  #{{ section.id }} .section-inner {
    padding-top: {{ section.settings.padding_top | append : 'px' }};
    padding-bottom: {{ section.settings.padding_bottom | append : 'px' }};
  }
{% endstyle %}

<div id="{{ section.id }}" class="relative">
  <div class="section-inner">
    <nav>
      <ul style="display: flex;flex-direction: row;align-items: center;gap: 10px;">
        {% for link in section.settings.menu.links %}
          <li data-active="{{ link.active }}">
            <a href="{{ link.url }}">
              {{- link.title | escape -}}
            </a>
          </li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</div>
{% schema %}
{
  "name": "t:sections.header",
  "tag": "section",
  "class": "section header",
  "enabled_on": {
    "groups": ["header"]
  },
  "settings":  [
    {
      "type": "select",
      "id": "container",
      "label": "Container",
      "default": "default",
      "options": [
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "full-width",
          "label": "Full Width"
        }
      ]
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:fields.menu",
      "default": "main-menu"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:fields.padding_top",
      "default": 20,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:fields.padding_bottom",
      "default": 20,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name":  "t:sections.header"
    }
  ]
}
{% endschema %}