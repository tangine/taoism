<div>
  {% if section.settings.header_menu %}
    <ul>
    {% for link in section.settings.header_menu.links %}
      <li></li>
    {% endfor %}
  </ul>
  {% endif %}
</div>
{% schema %}
{
  "name": "t:names.header",
  "settings": [
    {
      "type": "link_list",
      "id": "header_menu",
      "label": "t:settings.menu"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "t:names.header"
    }
  ]
}
{% endschema %}