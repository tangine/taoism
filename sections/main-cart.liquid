{% style %}
  #{{ section.id }} .section-inner {
    padding-top: {{ section.settings.padding_top | append : 'px' }};
    padding-bottom: {{ section.settings.padding_bottom | append : 'px' }};
  }
{% endstyle %}

{% liquid
%}


<div id="{{ section.id }}" class="relative">
  <div class="section-inner">
    <cart-info>
      {% unless cart.empty %}
        <table>
          <thead>
          <tr>
            <th>产品</th>
            <th>价格</th>
            <th>数量</th>
            <th>总计</th>
          </tr>
          </thead>
          <tbody>
            {% for item in cart.items %}
              <tr>
                <td>{{ item.title }}</td>
                <td>{{ item.price | money }}</td>
                <td>{% render 'quantity-input', quantity: item.quantity %}</td>
                <td>{{ item.final_line_price | money }}</td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      {% endunless %}
    </cart-info>
  </div>
</div>


{% schema %}
{
  "name": "t:sections.cart",
  "tag": "section",
  "class": "section cart",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings":  [
    {
      "type": "select",
      "id": "container",
      "label": "Container",
      "default": "default",
      "options": [
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "full-width",
          "label": "Full Width"
        }
      ]
    },
    {
      "type": "checkbox",
      "id": "only_show_selected_variant_image",
      "label": "t:settings.only_show_selected_variant_image",
      "default": false
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:settings.padding_top",
      "default": 20,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:settings.padding_bottom",
      "default": 20,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    }
  ],
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "@theme"
    }
  ],
  "presets": [
    {
      "name":  "t:sections.cart",
      "blocks": {

      },
      "block_order": [
      ]
    }
  ]
}
{% endschema %}


